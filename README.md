# install command
```
pkg update && pkg upgrade
pkg install python
pip install requests
pip install bs4
pip install rich

```
# RUN BOT
```
git clone https://github.com/XieraaaYEAGER/Foreign
cd Foreign
ls
git pull
python Foreign.py
